---
- hosts: all
  become: yes
  remote_user: root
  gather_facts: False


  vars_files:
    - vars.yml
  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600
  roles:
    #- init
    #- docker
    #- nginx
    #- jupyterlab
    - gitrepos

